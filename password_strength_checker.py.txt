password = input("Enter a password: ")

if len(password) < 6:
    print("Weak Password")
elif any(ch.isdigit() for ch in password) and any(ch.isalpha() for ch in password):
    print("Strong Password")
else:
    print("Medium Password")